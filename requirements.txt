# Core dependencies
pyyaml==6.0.1
numpy==1.26.4

# Vector operations (Phase 02)
# faiss-cpu>=1.7.4  # optional: pip install faiss-cpu
sentence-transformers==2.3.1

# GPU monitoring (required for VRAM probing)
# pynvml is deprecated — use nvidia-ml-py instead
nvidia-ml-py>=12.0.0
psutil==5.9.8

# LLM integration (Phase 06)
# openai>=1.0.0
# anthropic>=0.18.0

# Ollama (local LLM - no extra dependencies needed, uses urllib)
# Ollama must be installed separately: https://ollama.ai

# Testing
pytest==8.1.1
pytest-asyncio==0.23.5

# Development
black==24.3.0
ruff==0.3.5
